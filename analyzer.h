#ifndef __ANALYZER_H
#define __ANALYZER_H

#include "list.h"

int in_bg(list* lst);
char* input_red(list* lst);
char* output_red(list* lst);
char* append_red(list* lst);
int cnt_progs(list* lst);

#endif